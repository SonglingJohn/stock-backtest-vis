{
  "outputDirectory": "./frontend",
  "installCommand": "pip install -r requirements.txt && pip install -r cloud-functions/requirements.txt",
  "buildCommand": "python scripts/prepare_edgeone.py",
  "cloudFunctions": {
    "python": {
      "maxDuration": 120
    },
    "mainlandRegions": ["ap-guangzhou"]
  }
}
