{
  "outputDirectory": "./frontend",
  "installCommand": "python3 -m ensurepip --upgrade && (python3 -m pip install -r requirements.txt || test $? -eq 126)",
  "buildCommand": "python3 scripts/prepare_edgeone.py",
  "cloudFunctions": {
    "python": {
      "maxDuration": 120
    },
    "mainlandRegions": ["ap-guangzhou"]
  }
}
